# LakeOfFire

A description of this package.

## Development

- Install `mise` (recommended): `brew install mise` (or follow the official install docs).
- Generate/update the Brave SPM package from `brave-core`:
  - From this directory: `mise run brave-build`
  - From repo root: `mise run brave-build` (delegates here)
